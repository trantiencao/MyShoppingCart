<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vn.edu.ntu.trantiencao.myshoppingcart">

    <application
        android:name="vn.edu.ntu.trantiencao.controller.CartController"
        android:allowBackup="true"
        android:icon="@drawable/ic_action_cart_foreground"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_action_cart_foreground"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>